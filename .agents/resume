#!/usr/bin/env bash
set -euo pipefail

# This repository has no local services or non-interactive authentication to
# restore. User-specific Convex authentication remains outside orb lifecycle.
echo "No orb resume actions required"
